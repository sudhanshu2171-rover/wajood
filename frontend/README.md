# WAJOOD frontend

The independent frontend will mirror the current WAJOOD terminal while depending only on a local `/api` HTTP contract. No broker credentials or AppDeploy SDK are permitted here.

Migration baseline: AppDeploy snapshot `1789624590125`.
